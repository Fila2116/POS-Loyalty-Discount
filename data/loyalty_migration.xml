<odoo>
    <data noupdate="1">

        <record id="init_loyalty_points" model="ir.actions.server">
            <field name="name">Init Loyalty Points</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code">
partners = env["res.partner"].search([])
for p in partners:
    orders = env["pos.order"].search([("partner_id", "=", p.id)])
    total = sum(orders.mapped("amount_total"))
    p.loyalty_points = int(total // 100)
            </field>
        </record>

    </data>
</odoo>
